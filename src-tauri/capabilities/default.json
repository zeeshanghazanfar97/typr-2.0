{
  "identifier": "default",
  "description": "Default capabilities",
  "windows": ["main", "overlay", "notes"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "global-shortcut:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/whisper-cpp",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
